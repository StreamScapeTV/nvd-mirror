## Summary

Describe the behavior change and why it is needed.

## Validation

- [ ] Tests added or updated
- [ ] `pytest -q` passes
- [ ] `python -m compileall -q app tests` passes
- [ ] `bash -n scripts/*.sh` passes
- [ ] No credentials, runtime data, downloaded feeds, or private certificates are committed
