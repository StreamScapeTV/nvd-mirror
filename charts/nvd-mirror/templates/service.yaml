apiVersion: v1
kind: Service
metadata:
  name: {{ include "nvd-mirror.fullname" . }}
  labels:
    {{- include "nvd-mirror.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nvd-mirror.selectorLabels" . | nindent 4 }}
